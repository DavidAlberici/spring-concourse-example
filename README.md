# spring-concourse-example
Simple Spring project that will be built and deployed by Concourse, every time I make a commit

It will also consume a message from a simple RabbitMQ broker running in local
